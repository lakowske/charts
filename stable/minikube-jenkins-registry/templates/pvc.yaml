kind: PersistentVolume
apiVersion: v1
metadata:
  name: jenkins-pvc
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ .Values.persistence.hostpath }}"
